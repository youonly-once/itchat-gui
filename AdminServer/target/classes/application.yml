spring:
  application:
    name: AdminServer
server:
  port: 9999
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8888/eureka
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: ["*"]